<%= for post <- @posts do %>
	<div class="post">
		<a class="post__title" href="<%= post.slug %>"><h2><%= post.title %></h2></a>
		<%= raw Blog.Helpers.shorten_content post.content %>
		<br />
		<a class="post__link" href="<%= post.slug %>">Read more</a>
	</div>
<% end %>
