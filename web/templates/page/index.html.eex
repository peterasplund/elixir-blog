<%= for post <- @posts do %>
	<div class="post">
		<a class="post__title" href="<%= post.slug %>"><h2><%= post.title %></h2></a>
		<%= raw post.content %>
		<br />
		<a href="<%= post.slug %>">Read more</a>
	</div>
<% end %>